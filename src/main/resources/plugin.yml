name: HynseBackup
version: '${version}'
main: xyz.hynse.hynsebackup.HynseBackup
api-version: 1.19
commands:
  backup:
    description: Start a backup for a specified world
    usage: /<command> [start/list] <world>
folia-supported: true